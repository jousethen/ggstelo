import type { __title__(pascalCase)Props } from "./interfaces";
import styles from "./__title__.module.css"

export default function __title__(pascalCase)({}: __title__(pascalCase)Props): JSX.Element {
  return (
    <div className={styles["__title__"]}>

    </div>
  );
}
